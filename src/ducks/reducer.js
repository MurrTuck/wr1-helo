// import axios from 'axios'

const initialState = {
    username: '',
    id: '',
    profilePic: ''
}


export default function (state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}